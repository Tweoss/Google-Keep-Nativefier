Modifications to google keep using nativefier to make usage more ergonomic.

nativefier --name 'Google Keep Desktop' --icon application.icns "https://keep.google.com/" --inject inject.js --inject inject.css;
